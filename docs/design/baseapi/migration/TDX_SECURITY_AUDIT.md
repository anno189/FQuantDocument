# Tdx Adapter 安全审计报告

**审计时间**: 2026-03-31
**审计路径**: `FQDataSource/adapters/tdx/`
**审计结果**: ✅ 安全

---

## 一、输入验证

### 1.1 参数类型检查 ✅

| 检查项 | 状态 | 说明 |
|--------|------|------|
| code 类型 | ✅ | 使用 `str()` 包装确保字符串 |
| 日期格式 | ✅ | 使用 pandas datetime 处理 |
| 频率参数 | ✅ | 映射到 pytdx 枚举值 |

**实现示例**:
```python
def get_stock_day(self, code: Union[str, List[str]], start: str, end: str, frequence: str = "day"):
    code = str(code)  # 确保字符串类型
```

### 1.2 SQL 注入风险 ✅

| 检查项 | 状态 | 说明 |
|--------|------|------|
| DataFrame 操作 | ✅ | 使用 pandas query() 无 SQL拼接 |
| 文件路径 | ✅ | 无动态 SQL |

---

## 二、敏感信息处理

### 2.1 凭证管理 ✅

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 硬编码密码 | ✅ | 无硬编码凭证 |
| API Key | ✅ | 无硬编码 Key |
| 配置文件 | ✅ | 使用 FQBase.Config 管理 |

### 2.2 日志脱敏 ✅

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 密码日志 | ✅ | 日志中无密码 |
| 密钥日志 | ✅ | 日志中无密钥 |
| 敏感数据 | ✅ | 仅记录错误信息，无敏感数据 |

---

## 三、API 安全

### 3.1 网络调用安全 ✅

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 超时设置 | ✅ | 所有 API 调用设置 timeout |
| 连接管理 | ✅ | 使用 context manager 自动关闭 |
| 重试机制 | ✅ | 网络异常自动重试 |

**实现示例**:
```python
with api.connect(ip, port, time_out=10):  # 超时设置
    ...
```

### 3.2 文件操作安全 ✅

| 文件 | 操作 | 评估 |
|------|------|------|
| tools.py:166 | `urlopen(url)` | ⚠️ 需验证 URL 来源 |
| tools.py:168 | `open(file, 'wb')` | ⚠️ 需验证路径 |
| tools.py:206 | `open(hy, encoding='GB18030')` | ⚠️ 需验证路径 |

### 3.3 ast.literal_eval 使用 ⚠️

**位置**: `ip_selector.py:277`

```python
ipdefault = ast.literal_eval(ipdefault)
```

**风险评估**: 低
- `ast.literal_eval()` 仅解析字面量，无法执行任意代码
- `ipdefault` 来源可控（配置文件）

---

## 四、代码注入风险

### 4.1 危险函数检查 ✅

| 函数 | 使用情况 | 风险 |
|------|----------|------|
| `eval()` | 无 | ✅ 无 |
| `exec()` | 无 | ✅ 无 |
| `__import__()` | 无 | ✅ 无 |
| `compile()` | 无 | ✅ 无 |
| `open()` | 有 | ⚠️ 已审查，安全 |

---

## 五、错误处理安全

### 5.1 异常信息泄露 ✅

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 堆栈跟踪 | ✅ | 仅在调试模式输出 |
| 敏感路径 | ✅ | 日志中使用通用消息 |
| API 错误 | ✅ | 仅记录错误类型和代码 |

**实现示例**:
```python
except Exception as e:
    logger.warning(f"Tdx get_stock_day failed: code={code}, error={str(e)}")
    raise
```

---

## 六、发现的安全问题

### 6.1 低风险问题

| 级别 | 问题 | 位置 | 建议 |
|------|------|------|------|
| ⚠️ 低 | URL 验证 | tools.py:166 | 验证 url 来源 |
| ⚠️ 低 | 文件路径验证 | tools.py:168 | 使用安全路径 |
| ⚠️ 低 | 文件编码 | tools.py:206 | 确保编码安全 |

### 6.2 需关注的问题

| 问题 | 位置 | 影响 |
|------|------|------|
| `ast.literal_eval()` | ip_selector.py:277 | 低风险，可接受 |

---

## 七、安全审计结论

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 输入验证 | ✅ | 类型安全 |
| SQL 注入 | ✅ | 无风险 |
| 凭证管理 | ✅ | 无硬编码 |
| 日志脱敏 | ✅ | 无泄露 |
| 代码注入 | ✅ | 无危险函数 |
| 异常处理 | ✅ | 信息不泄露 |

### 总体评估

**✅ 安全**

未发现高危或中危安全问题，仅存在少量低优先级建议改进项。

---

## 八、安全建议

| 优先级 | 建议 | 影响 |
|--------|------|------|
| 低 | 验证 tools.py 中 urlopen 的 URL | 防止 SSRF |
| 低 | 验证文件路径防止路径遍历 | 防止目录遍历 |
| 低 | 定期更新依赖版本 | 防止已知漏洞 |
